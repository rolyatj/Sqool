
struct Player {
    var name: String
    var accuracy: Int
    var distance: Int
    var putting: Int
    var shortGame: Int
    var recovery: Int
    var overallRating: Int

    // Additional methods or computed properties if needed
}
